import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.hlh.engenharia',
    appName: 'HLH Engenharia',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
